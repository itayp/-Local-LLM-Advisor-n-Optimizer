# Keep line endings LF everywhere so gofmt, embedded files and the UI build
# produce the same bytes on all three operating systems.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
*.gguf binary
